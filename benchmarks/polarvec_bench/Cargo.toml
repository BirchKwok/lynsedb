[package]
name = "polarvec_bench"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = { version = "0.8", features = ["small_rng"] }
rayon = "1.10"
serde = { version = "1", features = ["derive"] }
half = "2"

[profile.release]
opt-level = 3
codegen-units = 1
lto = "thin"
